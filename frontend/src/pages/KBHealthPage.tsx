/** Legacy — redirected to knowledge quality / system operations (UIUX §7). */
import { Navigate } from 'react-router-dom'

export default function KBHealthPage() {
  return <Navigate to="/knowledge/quality" replace />
}
