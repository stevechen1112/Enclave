# ========================================================
# [DEPRECATED] — Admin Domain Isolation Config
# ========================================================
# This file is NO LONGER NEEDED. Admin functionality is
# now built into the main frontend (admin role sees admin
# menus automatically). There is no separate admin-frontend.
#
# For production deployment, use:
#   - Docker:      nginx/gateway.conf  (via docker-compose.prod.yml)
#   - Bare-metal:  nginx/client.conf   (single domain, all roles)
#
# This file is kept for reference only. Do NOT deploy it.
# ========================================================
