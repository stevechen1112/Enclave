# Mobile client — experimental workspace (DD-M13)

此目錄**不是** Enclave 2.0 GA 核心發布路徑。

- 無 `package-lock.json`／無正式 Mobile CI 時，不得視為可重現建置。
- 核心產品以 `frontend/`（Web）為準。
- 若日後納入 2.0：需 lockfile、Expo doctor、TypeScript check、Android build CI。

在此之前：變更本目錄不阻斷後端／Web GA 閘門。
