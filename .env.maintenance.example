# Enclave audited cross-tenant maintenance identity.
# Copy to .env.maintenance with mode 0600. Inject only into maintenance-capable worker jobs.
MAINTENANCE_POSTGRES_USER=enclave_maintenance
MAINTENANCE_POSTGRES_PASSWORD=       # REQUIRED: distinct strong password
