# ADR-003：單客戶地端部署與未來 SaaS 相容邊界

**狀態**：已接受（v2，2026-08-04 修訂）
**日期**：2026-07-31（v1）；2026-08-04（v2）
**決策者**：Enclave 技術團隊

---

## v2 修訂說明（2026-08-04）

`docs/CLOUD_AND_COMMERCIALIZATION_PLAN.md` 已正式採納（Accepted），本 ADR 邊界隨之擴張。**地端單客戶路線不變、永不刪除**；改變的是「明確不做事項」從永久禁令改為分階段允許：

| v1 決策 | v2 修訂 |
|---------|---------|
| 不預先實作計費／試用／自助註冊 | **計費**：Phase 1 直接接 NewebPay（D3 定案）；**自助註冊**：12 個月內仍不開（D5 定案），Sales-Led 開戶 |
| 不引入共享向量庫 | 形態 B 維持每客獨立；形態 C 允許**邏輯共享基礎設施**，但寫入真相（pgvector 於自家 PG）與 ACL 強度不放寬（D4 定案：向量維持 pgvector） |
| 不為 SaaS 做多區域 | Phase 3 允許（合規驅動）；Phase 1–2 單區域 |
| 不引入 K8s | Phase 1–2 維持 Compose；Phase 3 才評估 |

新增原則：

6. **三形態並存**：A 地端自管／B 託管私有雲／C 多租戶 SaaS，同一套程式碼，以 `STORAGE_BACKEND`／部署 profile 區分，禁止程式分叉（風險 R6）。
7. **雲端形態零 GPU**：B／C 的解析為 RAGFlow CPU 容器池＋掃描件路由 Mistral OCR API；GPU 僅存在於形態 A。
8. **隔離工程嚴謹性**（使用者明確要求）：形態 C 的租戶隔離採 shadow→enforce 分階段、繞過攻擊測試全綠才准 enforce、CG-RLS／CG-SIDECAR-MT 未過不得對 C 招商。見 ADR-012／ADR-013。
9. **維運由 AI agent 執行**：車隊維運 agent 化，人類僅批准例外（WS-AGENTIC-OPS；風險 R10 護欄：破壞性操作需批准＋完整稽核＋回滾優先）。

---

## v1 原文（保留備查）

## 背景

Enclave 首發販售模式為每客戶一套地端部署（Single-Customer On-Premise）。但未來可能擴展為多租戶 SaaS。需要在架構上預留相容性，避免日後大規模重構。

## 決策

**第一版以單租戶地端為設計目標，但資料模型與 API 保留多租戶相容性。**

### 具體措施

1. **資料模型**：所有核心表保留 `tenant_id` 欄位（即使初期只有一個租戶）。不對單租戶做 schema 簡化。
2. **API**：所有端點接受 `tenant_id` 參數（從 JWT 提取），不對單租戶做省略。
3. **部署**：第一版 Docker Compose 為單節點部署。不引入 Kubernetes 或 multi-master DB。
4. **隔離**：初期使用 PostgreSQL schema 級隔離（`tenant_id` WHERE 子句）。不引入實體資料庫隔離。
5. **授權**：授權模組支援 `tenant_id` 維度，即使初期只有一個租戶。

### 明確不做事項（v1；v2 已修訂，見上表）

- 不預先實作多租戶 SaaS 的計費、試用、自助註冊。
- 不引入跨租戶的資源池或共享向量庫。
- 不為 SaaS 做多區域部署。

## 理由

1. **避免日後重構**：`tenant_id` 欄位在初期幾乎零成本，但日後加入的成本極高。
2. **客戶接受度**：地端客戶通常要求單一部署實體，但資料模型的多租戶設計不影響他們。
3. **漸進式演進**：若日後推出 SaaS 版，核心資料模型無需變更。

## 後果

- 所有 SQL 查詢需帶 `tenant_id` 過濾（即使只有一個租戶）。
- 測試需涵蓋多租戶場景（即使初期不部署多租戶）。
